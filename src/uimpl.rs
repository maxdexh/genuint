// Internal types implementing the `Uint` trait.
// Names are as short as possible to keep them
// readable as binary numbers in error messages.
//
// These are private because they may be changed in the future.
pub struct _1;
pub struct _0;
pub struct A<H, P>(H, P);
