[workspace]
package.version = "0.1.0"
members = ["proc"]

[package]
version.workspace = true
name = "genuint"
edition = "2024"

[dependencies]
const-util = "2.2.0"
type-const = "1.1.1"
genuint-proc = { path = "proc" }


[features]
alloc = []
std = ["alloc"]
